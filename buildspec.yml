version: 0.2

phases:

  install:
    runtime-versions:
      nodejs: 22

    commands:
      - echo Installing dependencies

  build:
    commands:
      - echo Running tests
      - npm test
      - echo Building application
      - npm run build

artifacts:
  files:
    - '**/*'
